#include <stdio.h>
#include <stdlib.h>

int main(void){
    char mapa [5] [10];
    FILE* f;
    f = fopen("mapa.txt","r");
    if (f == 0){
        printf("Erro ao abrir o mapa");
        exit(1);
    }

    for (int i = 0)
    {
        /* code */
    }
    
}